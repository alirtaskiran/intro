import React, { Component } from "react"; // Komponent oluşturmak için React kütüphanesinden Component sınıfını import ettik.

export default class Navi extends Component {
  // Export : Public
  render() {
    return (
      <div>
        <h2>Navi Component</h2>
      </div>
    );
  }
}
